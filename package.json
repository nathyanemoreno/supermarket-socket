{
  "name": "supermarket-socket",
  "version": "0.0.1",
  "description": "A socket application for a supermarket.",
  "main": "server.ts",
  "author": "Nathyane Moreno",
  "license": "MIT",
  "private": false,
  "scripts": {
    "client:dev": "NODE_ENV=development nodemon src/client.ts",
    "client:start": "NODE_ENV=production ts-node src/client.ts",
    "server:dev": "NODE_ENV=development nodemon src/server.ts",
    "server:start": "NODE_ENV=production ts-node src/server.ts",
    "build": "tsc --project ./"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
